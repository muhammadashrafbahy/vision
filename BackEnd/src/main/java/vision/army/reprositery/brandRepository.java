package vision.army.reprositery;

import org.springframework.data.jpa.repository.JpaRepository;
import vision.army.entity.brand;

public interface brandRepository extends JpaRepository<brand, Integer> {
}
